spring.application.name=cricketCards
server.port = 8080
server.servlet.context-path=/cricAttacks
spring.datasource.url=jdbc:postgresql://${DB_HOST}:5432/cricket
spring.datasource.username=${POSTGRES_USER}
spring.datasource.password=${POSTGRES_PASSWORD}
